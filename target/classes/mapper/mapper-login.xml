<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-fate">
	<insert id="insertloginDB" parameterType="lx.edu.springboot.vo.LoginVO">
		insert into login(user_id, user_password, user_name)
				values(#{userId}, #{userPassword}, #{userName})
	</insert>
	<update id="updateLoginDB" parameterType="lx.edu.springboot.vo.LoginVO">
		update login
		set user_name = #{userName}
			user_password = #{userPassword}
			user_id = #{userId}
		where id = #{id}
	</update>
</mapper>

<!-- 첫 생성 insert / update -->

