<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-fate">
	<select id="selectResultFate" parameterType="int" resultType="lx.edu.springboot.vo.ResultFateVO">
		select id,
			   user_name userName,
			   max_five maxFive,
			   tree,
			   fire,
			   soil,
			   gold,
			   water,
			   total_fate totalFate,
			   love_fate loveFate,
			   money_fate moneyFate,
			   health_fate healthFate,
			   job_fate jobFate
		from result_fate
		where id = #{id}
	</select>
	
	<select id="selectEachResultFate" parameterType="String" resultType="String">
		select ${fate_name} from result_fate
	</select>

	<insert id="insertInputFate" parameterType="lx.edu.springboot.vo.InputFateVO">
		insert into input_fate(user_name, birth_date, birth_time, gender)
			   values(#{userName}, #{birthDate}, #{birthTime}, #{gender})
	</insert>

	<insert id="insertResultFate" parameterType="lx.edu.springboot.vo.ResultFateVO">
		insert into result_fate(user_name, max_five, tree, fire, soil, gold, water, total_fate, love_fate, money_fate,
								health_fate, job_fate)
			   values(#{user_name}, #{max_five}, #{tree}, #{fire}, #{soil}, #{gold}, #{water}, #{total_fate},
			   #{love_fate}, #{money_fate}, #{health_fate}, #{job_fate})
	</insert>
</mapper>