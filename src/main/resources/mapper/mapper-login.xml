<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper-login">

    <insert id="insertLoginDB" parameterType="lx.edu.springboot.vo.LoginVO">
        INSERT INTO login (USER_ID, USER_PW, USER_NICKNAME)
        VALUES (#{userId}, #{userPassword}, #{userNickName})
    </insert>

    <update id="updateLoginDB" parameterType="lx.edu.springboot.vo.LoginVO">
        UPDATE login
        SET USER_NICKNAME = #{userNickName}
         
        WHERE USER_ID = #{userId}
    </update>

    <select id="selectUserById" parameterType="String" resultType="lx.edu.springboot.vo.LoginVO">
        SELECT USER_ID as userId, USER_PW as userPassword, USER_NICKNAME as userNickName
        FROM login
        WHERE USER_ID = #{userId}
    </select>

</mapper>